import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

const features = [
  CommonModule,
]

@NgModule({
  declarations: [],
  imports: [features],
  exports: [features]
})
export class FeaturesModule { }
